{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://din18599-ifc.de/schema/v4.0/catalogs/geg_reference_building",
  "title": "Entry-Schema: geg_reference_building",
  "description": "Zeilen des GEG-Referenzgebaeudes. Anders als bei DIN-Tabellen sind die Zahlenwerte hier rechtlich unkritisch — das GEG ist ein amtliches Werk nach Paragraph 5 UrhG. Sie stehen trotzdem noch nicht drin, weil sie belegt aus dem Gesetzestext uebernommen werden muessen.",
  "type": "object",
  "required": ["code", "name_de", "component_group", "value_source"],

  "properties": {
    "code": {
      "type": "string",
      "pattern": "^[0-9]+(\\.[0-9]+)?$",
      "description": "Zeilennummer im Referenzgebaeude, z.B. '1.1' oder '2'. Ziel des berechneten Feldes opening.geg_reference_row."
    },
    "name_de": { "type": "string" },
    "name_en": { "type": "string" },

    "component_group": {
      "type": "string",
      "enum": ["opaque", "transparent"],
      "description": "Opake Bauteile tragen nur einen U-Wert, transparente zusaetzlich einen g-Wert."
    },

    "quantity": {
      "type": "string",
      "enum": ["u_value", "u_value_and_g_value"],
      "description": "Welche Groessen diese Zeile vorgibt"
    },
    "unit": { "type": "string" },

    "value_source": {
      "type": "string",
      "enum": ["legal_table", "dwe_definition"],
      "description": "legal_table = Wert stammt aus der GEG-Anlage. Gemeinfrei, aber belegt zu uebernehmen."
    },
    "norm_cell": {
      "type": ["string", "null"],
      "description": "Fundstelle im Gesetz, Konvention 'GEG<Jahr>-A<Anlage>-Z<Zeile>'"
    },
    "value_pending": {
      "type": "boolean",
      "description": "true = Zahlenwert steht noch aus. Blockiert calc_ready fuer den GEG-Nachweis."
    },

    "u_value": {
      "type": ["number", "null"],
      "minimum": 0,
      "description": "U-Wert des Referenzbauteils [W/(m2K)]. null solange value_pending."
    },
    "g_value": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 1,
      "description": "g-Wert, nur bei component_group='transparent'"
    },

    "note": { "type": "string" }
  },

  "allOf": [
    {
      "if": {
        "properties": { "value_pending": { "const": false } },
        "required": ["value_pending"]
      },
      "then": {
        "required": ["u_value"],
        "description": "Sobald der Eintrag als befuellt gilt, muss mindestens der U-Wert dastehen."
      }
    }
  ]
}
